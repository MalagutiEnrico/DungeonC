{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug C",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/code/gioco.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}/code",
            "externalConsole": true,
            "MIMode": "gdb",
            "miDebuggerPath": "gdb",
            "preLaunchTask": "build c project"
        }
    ]
}
